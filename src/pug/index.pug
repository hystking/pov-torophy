extend base

block append meta
  //- Meta Infomations
  title POV Torophy
  meta(name="description" content="")
  meta(name="keywords" content="")

  //- Facebook OGP
  meta(name="og:title" content="")
  meta(name="og:description" content="")
  meta(name="og:type" content="website")
  meta(name="og:url" content="")
  meta(name="og:image" content="")
  meta(name="og:site_name" content="")

  //- Twitter Card
  meta(name="twitter:card" content="summary_large_image")
  meta(name="twitter:title" content="")
  meta(name="twitter:description" content="")
  meta(name="twitter:image:src" content="")

block content
  .buttons
    button#saveButton.save-button 保存
    button#loadButton.load-button 読み込み

  .config
    .config__row
      .config__heading 行[×8]:
      .config__data
        input#rowNumberInput(type="number" min="1" step="1" value="1")
    .config__row
      .config__heading 列[×8]:
      .config__data
        input#columnNumberInput(type="number" min="1" step="1" value="1")

  input#colorPickerInput.color-picker(type="color")

  #cells.cells
    .cells__row
      .cells__cell
      .cells__cell
      .cells__cell
      .cells__cell
    .cells__row
      .cells__cell
      .cells__cell
      .cells__cell
      .cells__cell

  .config
    .config__row
      .config__heading 送信ポート:
      .config__data
        select#comNameList.com-name-list
    .config__row
      .config__heading 転送レート[bps]:
      .config__data
        input#baudRate(type="number" min="1" step="1" value="9600")
    .config__row
      .config__heading 送信バッファの分割サイズ[bytes]:
      .config__data
        input#maxBufferSize(type="number" min="1" step="1" value="32")
    .config__row
      .config__heading 送信間隔[msec]:
      .config__data
        input#sendingInterval(type="number" min="1" step="1" value="500")

  .buttons
    button#sendButton.send-button 送信

  .logger#logger
